# env.example — Required environment variables for the Skills-First AI Assistant.
#
# Rename to .env and fill in values:
#   cp env.example .env
#
# NEVER commit .env to version control.

# --- Phoenix ---
SECRET_KEY_BASE=generate-with-mix-phx-gen-secret
PHX_HOST=localhost
PORT=4000

# --- Database (PostgreSQL) ---
# Production: full DATABASE_URL
DATABASE_URL=ecto://postgres:postgres@localhost:5432/assistant_prod
# Dev/test use config/dev.exs and config/test.exs defaults

# --- OpenRouter (LLM + STT) ---
OPENROUTER_API_KEY=sk-or-...

# --- OpenAI (optional fallback/system key) ---
OPENAI_API_KEY=sk-...
# OpenAI OAuth (Connect button) defaults to Codex-compatible settings for testing:
# - client_id: app_EMoamEEZ73f0CkXaXp7hrann
# - authorize/token: auth.openai.com OAuth endpoints
# - scope: openid profile email offline_access
# Override only if needed:
# OPENAI_OAUTH_CLIENT_ID=your-openai-oauth-client-id
# OPENAI_OAUTH_CLIENT_SECRET=your-openai-oauth-client-secret
# OPENAI_OAUTH_AUTHORIZE_URL=https://auth.openai.com/oauth/authorize
# OPENAI_OAUTH_TOKEN_URL=https://auth.openai.com/oauth/token
# OPENAI_OAUTH_SCOPE=openid profile email offline_access
# OPENAI_OAUTH_CODEX_COMPAT=true
# OPENAI_OAUTH_ORIGINATOR=synaptic-assistant
# OPENAI_OAUTH_REDIRECT_URI=http://localhost:4000/auth/callback
# OPENAI_OAUTH_FLOW=device
# OPENAI_OAUTH_ISSUER=https://auth.openai.com

# --- Google Service Account ---
# Path to service account JSON key file, or inline JSON string
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# --- ElevenLabs (TTS) ---
ELEVENLABS_API_KEY=xi-...
ELEVENLABS_VOICE_ID=your-voice-id

# --- Telegram Bot ---
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
TELEGRAM_WEBHOOK_SECRET=your-webhook-secret

# --- Google Chat ---
GOOGLE_CHAT_WEBHOOK_URL=https://chat.googleapis.com/v1/spaces/...

# --- HubSpot ---
HUBSPOT_API_KEY=pat-...

# --- Encryption (Cloak.Ecto) ---
# Generate with: :crypto.strong_rand_bytes(32) |> Base.encode64()
CLOAK_ENCRYPTION_KEY=base64-encoded-32-byte-key
