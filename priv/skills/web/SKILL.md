---
domain: "web"
description: "Search the public web with citations, fetch pages, and extract readable text."
---

# web

- `web.search` — search the web through OpenRouter/OpenAI and preserve citations
- `web.fetch` — fetch a page, extract readable text, and optionally save it
- `web.extract` — extract readable text from a page, optionally scoped by CSS selector
